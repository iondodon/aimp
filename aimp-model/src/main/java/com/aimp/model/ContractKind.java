package com.aimp.model;

/**
 * Identifies the supported handwritten contract shapes.
 */
public enum ContractKind {
    /** A handwritten interface contract. */
    INTERFACE,
    /** A handwritten abstract class contract. */
    ABSTRACT_CLASS
}
